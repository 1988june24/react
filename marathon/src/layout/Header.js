import React, { Component } from 'react';
import Navigation from '../components/Navigation';

const Header = () => {

	return(
         <div>
         	<Navigation/>
         </div>
		);
};

export default Header;